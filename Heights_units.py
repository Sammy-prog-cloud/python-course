inches = float(input('Type in the number of the inches >>> '))
foot = float(input('Type in the number of the foot >>> '))
if inches >= 1:
    print('The number of inches in centimetres is ', inches / 2.54)
if foot >= 1:
    print('The number of foot in inches is ', foot / 12)
